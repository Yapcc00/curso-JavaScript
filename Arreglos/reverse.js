/**Como el nombre sugiere, el método reverse() reserva la posición del elemento en un arreglo
 *  entonces el último elemento va en la primera posición
 *  y la primera en la última. */


const nombres = ['tom', 'alex', 'bob'];

nombres.sort(); // returns ["alex", "bob", "tom"]