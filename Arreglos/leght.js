/**La propiedad length en JavaScript se utiliza para obtener la cantidad de elementos de un array
 *  o la longitud de una cadena de texto.

Cuando se aplica a un array, 
length devuelve el número de elementos contenidos en dicho array. A continuación, se muestra un ejemplo:
*/

const numeros = [1, 2, 3, 4, 5];
const longitud = numeros.length;

console.log(longitud); // Output: 5
